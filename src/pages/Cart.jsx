import React from 'react';
import CartBlock from "../components/CartBlock/CartBlock";

const Cart = () => {
    return (
        <div>
            <CartBlock/>
        </div>
    );
};

export default Cart;